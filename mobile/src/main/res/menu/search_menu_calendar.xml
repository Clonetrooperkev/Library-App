<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:appcompat="http://schemas.android.com/apk/res-auto"
    xmlns:app="http://schemas.android.com/tools">

    <item
        android:id="@+id/menu_search"
        android:title="@string/menu_search"
        appcompat:actionViewClass="android.support.v7.widget.SearchView"
        appcompat:showAsAction="always"/>
    <item
        android:id="@+id/menu_range"
        android:orderInCategory="1"
        app:showAsAction="never"
        android:title="@string/date_range">
        <menu>
            <group android:checkableBehavior="none">
                <item android:id="@+id/range_start"
                    android:orderInCategory="31"
                    android:title="@string/range_start" />
                <item android:id="@+id/range_end"
                    android:orderInCategory="32"
                    android:title="@string/range_end" />
            </group>
        </menu>
    </item>



</menu>